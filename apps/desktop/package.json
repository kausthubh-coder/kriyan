{
  "name": "@kriyan/desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:debug": "tauri build --debug --bundles app",
    "dev:web": "cd ../../web && bun run dev",
    "build:web": "cd ../../web && bun run build:desktop"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.4"
  }
}
